# https://dart.dev/guides/libraries/private-files
# Created by `dart pub`
.dart_tool/

# Avoid committing pubspec.lock for library packages; see
# https://dart.dev/guides/libraries/private-files#pubspeclock.
pubspec.lock

example/build
example/lib/*.g.dart

# dual-target smoke build artifacts (bindings in generated/ ARE committed
# so the tests run without libclang/codegen)
tool/dual_target_smoke/build
